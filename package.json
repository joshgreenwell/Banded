{
  "name": "banded",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"esbuild server.js --platform=node --bundle --outfile=build/index.js --watch\" \"nodemon build/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.1",
    "esbuild": "^0.14.42",
    "nodemon": "^2.0.16",
    "prettier": "^2.6.2"
  },
  "dependencies": {
    "express": "^4.18.1",
    "mongodb": "^4.6.0"
  }
}
